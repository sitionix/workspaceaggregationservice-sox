name: Service Deploy Run
description: Build, publish, deploy, and verify the workspace aggregation service on the target VM.

inputs:
  github_environment:
    description: GitHub Environment name used to generate the release id.
    required: true

outputs:
  release_id:
    description: Generated release id.
    value: ${{ steps.release.outputs.release_id }}
  image_ref:
    description: Published immutable image reference.
    value: ${{ steps.release.outputs.image_ref }}

runs:
  using: composite
  steps:
    - name: Validate deploy environment contract
      shell: bash
      run: |
        for required_name in \
          DEPLOY_VM_HOST \
          DEPLOY_VM_USER \
          DEPLOY_VM_SSH_PRIVATE_KEY \
          GHCR_PULL_USERNAME \
          GHCR_PULL_TOKEN \
          MAVEN_REPOSITORY_USERNAME \
          MAVEN_REPOSITORY_TOKEN \
          WAGS_SOX_DB_PASSWORD; do
          if [[ -z "${!required_name:-}" ]]; then
            echo "Missing required deploy value: ${required_name}" >&2
            exit 1
          fi
        done

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: "21"
        cache: maven

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Materialize Maven settings
      uses: ./.github/actions/materialize-maven-settings
      with:
        maven_repository_username: ${{ env.MAVEN_REPOSITORY_USERNAME }}
        maven_repository_token: ${{ env.MAVEN_REPOSITORY_TOKEN }}
        output_path: .maven-settings.xml

    - name: Log in to GHCR for image publish
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ github.token }}

    - name: Prepare release metadata
      id: release
      shell: bash
      env:
        GITHUB_ENVIRONMENT_NAME: ${{ inputs.github_environment }}
      run: |
        owner="$(printf '%s' "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
        short_sha="$(git rev-parse --short HEAD)"
        release_id="${GITHUB_ENVIRONMENT_NAME}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-${short_sha}"
        image_ref="ghcr.io/${owner}/workspaceaggregationservice-sox:${GITHUB_SHA}"

        echo "release_id=${release_id}" >> "$GITHUB_OUTPUT"
        echo "image_ref=${image_ref}" >> "$GITHUB_OUTPUT"

    - name: Build and publish runtime image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: ${{ steps.release.outputs.image_ref }}
        secret-files: |
          maven_settings=${{ github.workspace }}/.maven-settings.xml

    - name: Prepare deployment payload
      shell: bash
      env:
        GITHUB_ENVIRONMENT_NAME: ${{ inputs.github_environment }}
        RELEASE_ID: ${{ steps.release.outputs.release_id }}
        IMAGE_REF: ${{ steps.release.outputs.image_ref }}
      run: |
        payload_root=".deploy/release/payload"
        mkdir -p "${payload_root}/bin"

        cp deploy/service/vm/deploy-service.sh "${payload_root}/bin/deploy-service.sh"
        chmod +x "${payload_root}/bin/deploy-service.sh"

        cat <<EOF > "${payload_root}/release.env"
        SITIONIX_RELEASE_ID=${RELEASE_ID}
        SITIONIX_IMAGE_REF=${IMAGE_REF}
        SITIONIX_CONTAINER_NAME=workspaceaggregationservice-sox
        SITIONIX_NETWORK_ALIAS=workspaceaggregationservice-sox
        SITIONIX_BIND_ADDRESS=127.0.0.1
        SITIONIX_HOST_PORT=9082
        SITIONIX_CONTAINER_PORT=9082
        SITIONIX_ACTIVE_PROFILE=${GITHUB_ENVIRONMENT_NAME}
        SITIONIX_DOCKER_NETWORK=sitionix-${GITHUB_ENVIRONMENT_NAME}
        SITIONIX_RUNTIME_ROOT=/opt/sitionix/runtime/workspaceaggregationservice-sox
        SITIONIX_BACKUP_ROOT=/opt/sitionix/backups/workspaceaggregationservice-sox
        SITIONIX_SHARED_SECRET_ENV_PATH=/opt/sitionix/runtime/shared/${GITHUB_ENVIRONMENT_NAME}-internal-auth.env
        SITIONIX_SERVICE_ENV_PATH=/opt/sitionix/runtime/workspaceaggregationservice-sox/shared/workspaceaggregationservice-sox.${GITHUB_ENVIRONMENT_NAME}.env
        SITIONIX_LOCAL_READINESS_URL=http://127.0.0.1:9082/wagssox/actuator/health/readiness
        SITIONIX_LOCAL_HEALTH_URL=http://127.0.0.1:9082/wagssox/actuator/health
        SITIONIX_DB_URL=jdbc:postgresql://postgres:5432/wags_sox
        SITIONIX_DB_USERNAME=wagssox_app
        SITIONIX_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
        SITIONIX_KAFKA_CONSUMER_GROUP_ID=wagssox-sitemeta
        EOF

        cat <<EOF > "${payload_root}/release-manifest.json"
        {
          "releaseId": "${RELEASE_ID}",
          "imageRef": "${IMAGE_REF}",
          "githubSha": "${GITHUB_SHA}",
          "githubEnvironment": "${GITHUB_ENVIRONMENT_NAME}",
          "dockerNetwork": "sitionix-${GITHUB_ENVIRONMENT_NAME}",
          "containerName": "workspaceaggregationservice-sox",
          "privateHostPort": "127.0.0.1:9082",
          "privateBaseUrl": "http://workspaceaggregationservice-sox:9082/wagssox"
        }
        EOF

        secrets_file=".deploy/runtime-secrets.env"
        : > "${secrets_file}"
        for name in \
          GHCR_PULL_USERNAME \
          GHCR_PULL_TOKEN \
          WAGS_SOX_DB_PASSWORD; do
          value="${!name}"
          printf 'export %s=%q\n' "${name}" "${value}" >> "${secrets_file}"
        done
        chmod 0600 "${secrets_file}"

        tar -C "${payload_root}" -czf .deploy/service-release.tgz .

    - name: Configure SSH
      shell: bash
      run: |
        install -m 0700 -d ~/.ssh
        install -m 0600 /dev/null ~/.ssh/id_ed25519
        printf '%s\n' "$DEPLOY_VM_SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
        install -m 0600 /dev/null ~/.ssh/known_hosts

        known_hosts_tmp="$(mktemp)"
        if ssh-keyscan -T 10 -p "${DEPLOY_VM_PORT:-22}" -H "$DEPLOY_VM_HOST" > "${known_hosts_tmp}" 2>/dev/null && [[ -s "${known_hosts_tmp}" ]]; then
          cat "${known_hosts_tmp}" >> ~/.ssh/known_hosts
        else
          echo "Warning: unable to prefetch SSH host key for ${DEPLOY_VM_HOST}:${DEPLOY_VM_PORT:-22}; continuing with accept-new." >&2
        fi
        rm -f "${known_hosts_tmp}"

    - name: Upload payload to VM
      shell: bash
      env:
        RELEASE_ID: ${{ steps.release.outputs.release_id }}
      run: |
        remote_tarball="/tmp/${RELEASE_ID}.tgz"
        remote_secret_env="/tmp/${RELEASE_ID}.env"
        ssh_options=(-i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o ServerAliveInterval=15 -o ServerAliveCountMax=3)

        for attempt in $(seq 1 5); do
          if scp "${ssh_options[@]}" -P "${DEPLOY_VM_PORT:-22}" .deploy/service-release.tgz "${DEPLOY_VM_USER}@${DEPLOY_VM_HOST}:${remote_tarball}" \
            && scp "${ssh_options[@]}" -P "${DEPLOY_VM_PORT:-22}" .deploy/runtime-secrets.env "${DEPLOY_VM_USER}@${DEPLOY_VM_HOST}:${remote_secret_env}"; then
            exit 0
          fi
          sleep 3
        done

        echo "Unable to upload service payload to ${DEPLOY_VM_HOST}:${DEPLOY_VM_PORT:-22}" >&2
        exit 1

    - name: Run remote deployment
      shell: bash
      env:
        RELEASE_ID: ${{ steps.release.outputs.release_id }}
      run: |
        remote_root="/tmp/${RELEASE_ID}"
        remote_tarball="/tmp/${RELEASE_ID}.tgz"
        remote_secret_env="/tmp/${RELEASE_ID}.env"
        ssh_options=(-i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15 -o ServerAliveInterval=15 -o ServerAliveCountMax=3)

        ssh "${ssh_options[@]}" -p "${DEPLOY_VM_PORT:-22}" "${DEPLOY_VM_USER}@${DEPLOY_VM_HOST}" "
          set -euo pipefail
          cleanup() {
            rm -f '${remote_tarball}' '${remote_secret_env}'
            rm -rf '${remote_root}'
          }
          trap cleanup EXIT
          rm -rf '${remote_root}'
          mkdir -p '${remote_root}'
          tar -xzf '${remote_tarball}' -C '${remote_root}'
          chmod +x '${remote_root}/bin/deploy-service.sh'
          set -a
          . '${remote_secret_env}'
          set +a
          '${remote_root}/bin/deploy-service.sh'
        "

    - name: Verify private deployment
      shell: bash
      env:
        SITIONIX_RELEASE_ID: ${{ steps.release.outputs.release_id }}
      run: bash deploy/service/ci/verify-private-deploy.sh
